# Unix
alias ll="ls -al"
alias lt="ls -altr"
alias goreef="cd ~/workspace/reef"

alias gprs="git stash && git pull --rebase && git stash pop"

alias 'gpp'='git pull && git push'
alias 'gup'='git checkout main && git pull --rebase && git checkout -'
